import React from "react";

function NavBar() {
  // update the JSX being returned!
  return (<nav>
    <a href="home">home</a>
    <a href="about">about</a>
    NavBar</nav>)
}

export default NavBar;
